import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.fitsg.ftiretail',
  appName: 'SG Retail',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
